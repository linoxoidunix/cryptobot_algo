#include "aos/aos.h"

int main() {
    {
        // using NetTypes = aos::types::NetPositionTypes<>;

        // NetTypes::PositionStrategyContainerT container(1);
        // auto strategy = container.Build();
        // NetTypes::Position long_pos(strategy);
    }
    fmtlog::poll();
    return 0;
}